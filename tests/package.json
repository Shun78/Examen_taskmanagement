{
  "name": "task-manager-tests",
  "version": "1.0.0",
  "description": "Tests pour le gestionnaire de tâches",
  "scripts": {
    "test": "jest --testTimeout=30000",
    "test:unit": "jest tasks.test.js app.test.js",
    "test:api": "jest api.test.js",
    "test:e2e": "jest e2e.test.js",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "axios": "^1.13.6",
    "chromedriver": "^114.0.0",
    "jest": "^29.7.0",
    "selenium-webdriver": "^4.11.0"
  }
}
